#include<iostream>
#include<math.h>
using namespace std;
int sum(int x, int y){
    return x+y;
}
int main(){
    int a,b;
    cin>>a>>b;
    cout<<sum(a,b)<<endl;

    // Some inbuilt library functions
    min(a,b);
    max(a,b);
    sqrt(a);
    
}